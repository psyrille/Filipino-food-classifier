# BantayAllerji

A simple application that classifies local Filipino foods and prompts warning if the food classified contains an allergen based from the users profile.

The application uses the Dart language and Flutter framework. I used Google Teachable Machine for training the model. I also used python in augmenting the datasets.
